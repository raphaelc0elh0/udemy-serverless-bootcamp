createAuction:
  handler: src/modules/auctions/useCases/createAuction.handler
  events:
    - http:
        method: POST
        path: /auction
  iamRoleStatements:
    - Effect: Allow
      Action:
        - dynamodb:PutItem
      Resource:
        - ${self:custom.tables.AuctionsTable.arn}
